# Dependencias OPCIONALES para los backends no-default de PDF.
# NO son necesarias para arrancar: los adaptadores las importan de forma perezosa y los
# defaults (STORAGE_BACKEND=local, JOB_QUEUE=sync) no las tocan. Instálalas solo si activas
# el backend correspondiente.
#
#   STORAGE_BACKEND=s3  -> boto3   (almacenamiento S3/MinIO)
#   JOB_QUEUE=rq        -> rq      (cola de trabajos sobre Redis; `redis` ya está en requirements.txt)
#
# mysqlclient: driver NATIVO de MySQL. No hace falta: la app usa PyMySQL (Python puro, ya
# en requirements.txt) y config.py reescribe 'mysql://' a 'mysql+pymysql://'. Compilarlo
# exige cabeceras de MySQL y un compilador, lo que rompe la instalación en macOS y Windows
# sin aportar nada. Instálalo solo si necesitas el driver 'mysql+mysqldb://' a propósito.

boto3>=1.34
rq>=1.16
# mysqlclient>=2.2
